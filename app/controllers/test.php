<?php
use Biaoye\Model\Agent;
use Biaoye\Model\Customer;

// 获取短信验证码
$app->post('/test/product/add', function () use ($app) {
    
});


$app->post('/test/agent/add', function () use ($app) {
    // $ar = new Agent();
    // $ar->phone = "1" . rand(1000000000, 9999999999);
    // $ar->sex = rand(0, 1);
    // $ar->school_id = 1;
    // $ar->room_id = 1;
});